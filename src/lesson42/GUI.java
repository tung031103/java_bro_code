package lesson42;

import java.awt.Color;

import javax.swing.ImageIcon;

import javax.swing.JFrame;

public class GUI {
    public static void main(String[] args) {
        // JFrame = a GUI window to add components to
        // JFrame frame = new JFrame(); // create a frame
        // frame.setTitle("GUI JFrame"); // set title of frame
        // frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE); // exist out of
        // application
        // frame.setResizable(false); // prevent frame from being resized
        // frame.setSize(420, 420); // set the x-dimension, and y dimension
        // frame.setVisible(true); // make framme visible

        // ImageIcon img = new
        // ImageIcon("C:\\myweb\\js_bro_code\\code_java\\lapTrinh_JAVA\\src\\lesson42\\tung.png");
        // frame.setIconImage(img.getImage());
        // frame.getContentPane().setBackground(new Color(255, 255, 0));

        new myFrame();
    }
}
